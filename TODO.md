# TODO

---

- [x] Move command line to the bottom, and it make its size fully scaling depending on window size
- [x] Separate eraser and brush sizes
- [ ] Add command ":colors" which opens color palette, start typing to hide
- [x] Issue: palette coords are invalid
- [x] Add action when hovering over a color and picking it, and add borders in general, to all els (canvas, cmd line, etc.)
- [ ] Add <SUPER+z> to undo recent change and <SUPER+SHIFT+z> to undo the undo
- [ ] Rearrange code into a single struct (struct Paint for example) to better manage parameters of functions

